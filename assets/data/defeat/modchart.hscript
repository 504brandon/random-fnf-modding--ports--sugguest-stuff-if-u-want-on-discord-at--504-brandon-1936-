function start(song) {
    //currentPlayState.switchCharacter();
    swapChar('dad', 'bf');
}

function update(elapsed) {
    //FlxG.sound.music.time += elapsed * 4 * 100;
    //currentPlayState.vocals.time += elapsed * 4 * 100;
}

function playerTwoSing() {
}